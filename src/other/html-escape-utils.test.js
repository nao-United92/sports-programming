import { escapeHTML, unescapeHTML } from './html-escape-utils';describe('escapeHTML', () => {  it('should escape HTML special characters', () => {    const str = '<div class="test"> & 'hello' >';    const expected = '&lt;div class=&quot;test&quot;&gt; &amp; &#39;hello&#39; &gt;';    expect(escapeHTML(str)).toBe(expected);  });  it('should not change a string with no special characters', () => {    const str = 'hello world';    expect(escapeHTML(str)).toBe(str);  });});describe('unescapeHTML', () => {  it('should unescape HTML entities', () => {    const str = '&lt;div class=&quot;test&quot;&gt; &amp; &#39;hello&#39; &gt;';    const expected = '<div class="test"> & 'hello' >';    expect(unescapeHTML(str)).toBe(expected);  });  it('should not change a string with no HTML entities', () => {    const str = 'hello world';    expect(unescapeHTML(str)).toBe(str);  });});