/**
 * Checks if the first number is divisible by the second.
 * @param {number} dividend Number to check.
 * @param {number} divisor Divisor.
 * @returns {boolean} True if divisible, false otherwise.
 */
export const isDivisible = (dividend, divisor) => dividend % divisor === 0;
